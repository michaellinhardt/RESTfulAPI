name: Push Main Branch to NPM Package

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm ci
    - run: npm test

  publish-npm:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 16.x
          registry-url: https://registry.npmjs.org/
      - run: npm ci
      - run: git config --global user.email "linhardt.michael@gmail.com"
      - run: git config --global user.name "Michael Linhardt"
      - run: npm version patch -m "Auto bump version to %s for NPM deploy CI"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} # this token doesnt need to be set, its auto filled by github
      - run: git push --follow-tags
      - run: npm publish
        env:
          NPM_AUTOMATION: ${{ secrets.NPM_AUTOMATION }}
